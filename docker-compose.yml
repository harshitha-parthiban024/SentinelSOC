services:
  mongo:
    image: mongo:7
    container_name: sentinelsoc-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - sentinelsoc-mongo-data:/data/db

volumes:
  sentinelsoc-mongo-data:
